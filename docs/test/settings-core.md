# 设置页核心回归

## 1. 关联能力

- 模型配置
- 语言切换
- 快捷输入配置
- 本地缓存
- 导入导出
- 云同步

## 2. 核心职责清单

- 正确编辑并保存当前已交付的配置子集。
- 只暴露可用模型候选。
- 支持快捷输入新增、编辑、软删除和排序。
- 支持中英文切换。
- 支持主题即时预览与保存后恢复。
- 支持模型行摘要、展开编辑和 Provider 差异字段编辑。
- 支持本地缓存占用查看与清理。
- 支持恢复默认配置并刷新页面状态。
- 支持导出配置并把结果交给用户。
- 支持同步配置编辑、连接测试和手动同步。
- 阶段2流程回归覆盖设置页本地闭环和配置级手动同步，不把页面/会话远端合并当作已完成能力。
- 阶段2流程回归必须经过 `options -> chrome.runtime.sendMessage -> background -> repository` 的真实命令链路，不允许用页面内 mock 替代 background 处理。

## 3. 验证点

- 正常流：
  - 左侧导航固定为 5 个栏目，顶部动作区固定为“保存 / 恢复默认 / 导入配置 / 导出配置”。
  - 在“基础设置”里编辑 `System Prompt` 后切换到其他栏目，再切回时草稿仍保留。
  - 模型面板支持新增、复制、软删除和顺序调整，并通过保存真正写回配置。
  - 模型行展开后编辑显示名称、Provider、模型标识和差异字段并成功保存。
  - API Key 默认掩码显示，切换查看后不影响保存值。
  - 快捷输入列表会过滤软删除项并保持顺序。
  - 快捷输入支持编辑名称、提示词、自动触发和专属模型。
  - 快捷输入引用失效模型时前端有降级提示，但不导致保存失败。
  - 默认模型候选只显示“启用且配置完整”的模型。
  - 切换语言并保存。
  - 切换语言后立即预览，点击保存后刷新页面仍保持已保存语言。
  - 切换主题后立即预览，点击保存后刷新页面仍保持已保存主题。
  - 点击恢复默认后，语言、主题和默认配置同步回到默认值。
  - 正确展示本地缓存占用并执行清理。
  - 导出配置后生成可下载文件。
  - 导入配置后恢复当前页面状态。
  - 云同步分栏支持启用同步、切换 provider、展示对应凭据字段。
  - 测试连接成功后反馈结果可见。
  - 立即同步成功后反馈快照大小并回写最近同步时间。
- 错误流：
  - 保存失败。
  - 导入结构非法。
  - 导入失败后错误可见且当前草稿不被覆盖。
  - 连接测试失败。
  - 手动同步失败。
  - background 返回业务错误时前端正确报错。
  - Provider 切换后缺少必填字段时阻止保存。
  - 导出失败时错误可见。
  - 缓存清理失败时错误可见且不破坏当前表单。
- 异常流：
  - 快速重复保存。
  - 基础设置草稿切换栏目后不丢失。
  - 模型展开项切换到其他模型后再切回，未保存内容仍在当前表单态中。

## 4. 关键边界条件

- 模型不完整但启用。
- 快捷输入 ID 冲突。
- 语言资源缺失 key。
- Provider 切换后旧字段残留。

## 5. 历史高风险场景

- 排序后保存结果与 UI 不一致。
- 语言切换即时预览但未持久化。
- 删除配置项导致历史引用失效。
- API Key 掩码逻辑导致空值误覆盖真实值。
- 缓存清理误删仍需保留的配置数据。
- background 业务错误被前端误判为成功。
- service worker 启动后注入的测试 provider 未被同步命令读取，导致 E2E 误走真实网络分支。

## 6. 测试数据与环境

- 中英文语言资源文件
- 4 类 Provider 示例模型
- 导出配置示例数据
- Gist / WebDAV 同步测试桩

## 7. 验收补充

- 左侧导航固定为“基础设置 / 标签页 / 语言模型 / 云同步 / 黑名单设置”。
- 阶段2顶部操作固定为“保存 / 恢复默认 / 导出配置 / 导入配置”。
- 语言模型页采用“列表摘要 + 展开编辑”形态，不使用整页跳转编辑。
- 模型级动作至少覆盖“新增 / 复制 / 软删除 / 顺序调整”，软删除项不再进入默认模型候选。
- 默认模型候选、侧边栏模型选择器、分支模型选择器对“启用且配置完整”的判定一致。
- 阶段2的“云同步”已交付最小配置级闭环；“标签页 / 黑名单设置”仍只验证导航与已交付内容，不视为完整编辑能力。
- 阶段 2.5 额外要求设置页根节点保留 `data-testid="settings-shell"`、`data-theme` 与主题 class，顶部动作区保留 `settings-shell-actions`，导航区保留 `settings-shell-nav`。
- 阶段 2.5 组件回归以 `tests/component/options/settings-shell.spec.tsx`、`tests/component/options/model-form.spec.tsx`、`tests/component/options/quick-inputs.spec.tsx` 为主，不允许用页面内 mock 绕过真实设置页壳层。
- 全局主题回归要求 `assets/styles/globals.css` 保持 `Sky` 主色、`Amber` 图表色与 `Radius: None` 令牌基线，视觉调整不应改动设置页命令链路和测试标识。
